# Sports guide event cards

## Change
- Remove the `01`, `02`, `03` sequence numbers from member-facing sports guide event cards.
- Use the freed space for the event name, channel details, date, time, and timezone already associated with each event.
- Keep the existing automatic date conversion, event sorting, and 10-hour expiry behaviour unchanged.
- Adjust spacing so event details align cleanly across phone and desktop layouts.

## Verification
- Check a guide with one event and a guide with several events.
- Confirm event names, channels, dates, times, and timezone labels remain visible and correctly ordered.
- Confirm the page still fits and scrolls correctly on narrow screens.
