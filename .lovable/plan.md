# Member home hero layout

## Changes
- Rebuild the member home’s main area as a balanced three-column row: wide image, hero text, and the working-status/subscription stack.
- Keep the welcome heading and both paragraphs fully inside the coloured hero.
- Move Expert Support and Service Status into a separate overlapping row so each block sits partly inside the hero and partly below it, matching the landing-page treatment.
- Stretch the image to fill its available width while keeping equal outer spacing on the image side and beside the right-hand status boxes.
- Preserve the existing quick links, role visibility, status behaviour, and all chat/ticket functionality.

## Responsive behaviour
- Use the balanced desktop layout shown in the reference screenshot.
- Stack content cleanly on narrower screens without clipped text or controls.

## Verification
- Check the member home at desktop and mobile widths, confirm equal outer spacing, visible overlap blocks, and no layout overflow.
- Confirm the preview build has no errors.
