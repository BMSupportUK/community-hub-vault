# Preserve line breaks in edited email templates

## Changes
- Make line breaks entered in the HTML email editor render as visible line breaks in its preview.
- Apply the same line-break handling to the HTML sent to recipients.
- Keep existing HTML formatting, image uploads, placeholders, and designed default templates unchanged.

## Verification
- Confirm a multi-line edited template keeps each entered line separate in preview and delivery output.
- Confirm the app builds cleanly.
